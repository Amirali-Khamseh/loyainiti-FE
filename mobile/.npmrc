# better-call (a Better Auth transitive) has a peerOptional on zod ^4, but the
# project uses zod ^3 (which Better Auth itself requires). The peer is optional,
# so it's safe to ignore. legacy-peer-deps makes npm behave like v6 here.
legacy-peer-deps=true
